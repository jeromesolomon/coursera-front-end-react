// shared data
import { LEADERLIST } from '../shared/leaderList';

export const LeaderListReducer = (state = LEADERLIST, action) => {
    switch(action.type) {
        default:
            return state;
    }
}
