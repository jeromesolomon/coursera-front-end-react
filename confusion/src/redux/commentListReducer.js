// shared data
import { COMMENTLIST } from '../shared/commentList';

export const CommentListReducer = (state = COMMENTLIST, action) => {
    switch(action.type) {
        default:
            return state;
    }
}
