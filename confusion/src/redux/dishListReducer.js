// shared data
import { DISHLIST } from '../shared/dishList';

export const DishListReducer = (state = DISHLIST, action) => {
    switch(action.type) {
        default:
            return state;
    }
}
