// shared data
import { PROMOTIONLIST } from '../shared/promotionList';

export const PromotionListReducer = (state = PROMOTIONLIST, action) => {
    switch(action.type) {
        default:
            return state;
    }
}
